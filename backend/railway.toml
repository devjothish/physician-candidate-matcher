[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
numReplicas = 1
startCommand = "python -c 'import app.main' && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
